var config = {
  "port": 3000,
  "api": {
    bus:{
      host: 'http://localhost',
      port: 5000
    },
    user:'user',
    video:'video',
    globalConfig:'globalConfig',
  },
  lastUser:null,
}

module.exports = config;